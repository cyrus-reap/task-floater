<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Enable App Sandbox (REQUIRED for Mac App Store) -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Allow read/write to user-selected files (for import/export) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow network access (for future Linear API integration) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow JIT compilation for JavaScript engine -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Screen capture permission for desktopCapturer API -->
    <key>com.apple.security.temporary-exception.mach-lookup.global-name</key>
    <array>
      <string>com.apple.screencaptureui</string>
    </array>
  </dict>
</plist>
