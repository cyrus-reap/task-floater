# Keyboard Shortcuts Reference

## 📝 Task Management

| Shortcut | Action |
|----------|--------|
| `Enter` | Add task (when in input field) |
| `Enter` | Save edit (when editing task) |
| `Esc` | Clear input / Cancel edit / Clear search |
| `Double-click` | Edit task title |

## 🎯 Navigation & Selection

| Shortcut | Action |
|----------|--------|
| `↑` | Select previous task |
| `↓` | Select next task |
| `Space` | Toggle complete (selected task) |
| `Enter` | Edit (selected task) |

## ⚡ Quick Actions

| Shortcut | Action |
|----------|--------|
| `Cmd+Shift+S` | Capture screenshot and extract tasks |
| `Cmd+K` | Open Command Palette |
| `Cmd+Shift+F` | Toggle Focus Mode |
| `Cmd+D` | Mark selected task complete |
| `Cmd+Backspace` | Delete selected task |
| `Cmd+F` | Focus search box |
| `Cmd+T` | Toggle light/dark theme |

## 📁 Data Management

| Shortcut | Action |
|----------|--------|
| `Cmd+E` | Export tasks to JSON file |
| `Cmd+I` | Import tasks from JSON file |

## 🪟 Window Controls

| Shortcut | Action |
|----------|--------|
| `Cmd+Q` | Quit app |
| Click yellow | Minimize to dock |
| Click red | Close app |
| Drag header | Move window |

## 💡 Pro Tips

1. **Navigate without mouse**: Use arrow keys + Space to check off tasks
2. **Quick edit**: Select with arrows, press Enter, type, press Enter again
3. **Batch delete**: Arrow down, Cmd+Backspace, repeat
4. **Focus mode**: Start with Cmd+F to clear search, then arrow through tasks
5. **Rapid entry**: Type task, Enter, type task, Enter (no clicking needed)

## 🎮 Complete Workflow Example

```
1. Cmd+F          → Focus search (to deselect)
2. Click input    → Ready to type
3. Type "Task 1"
4. Click "30 min" → Set duration
5. Enter          → Add with timer
6. Type "Task 2"
7. Enter          → Add without timer
8. ↓             → Select Task 1
9. Space          → Start timer
10. (wait...)
11. → Notification appears
12. Cmd+E         → Export tasks
```

## 🔍 Search Tips

- Search filters tasks in real-time
- Matching is case-insensitive
- Searches task title only
- Press Esc to clear search instantly
- Use Cmd+F to jump to search from anywhere

## 🎨 Theme Switching

- `Cmd+T` toggles between light and dark
- Preference saved automatically
- All colors adapt smoothly
- Works with system accessibility settings

## ⏱️ Timer Controls

While timers have on-screen buttons, you can:
- Use arrows to select task
- Press Enter to edit/focus
- Click timer buttons directly

No keyboard shortcuts for play/pause to avoid conflicts with text editing.

---

**Tip**: Print this reference or keep it handy while learning the shortcuts!
